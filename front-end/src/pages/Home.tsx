import React from "react";
import Auth from "../components/auth/Auth";

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <Auth />
    </React.Fragment>
  );
};

export default Home;
